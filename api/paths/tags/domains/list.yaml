get:
  tags:
    - tag domains
  summary: Returns a list of all tag domains
  responses:
    200:
      description: Json array
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../../schemas/TagDomainInfo.yaml'