get:
  tags:
    - tag domains
  summary: Returns all tag domains
  responses:
    "200":
      description: All tag domains, or an empty array
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../../schemas/TagDomainInfo.yaml'
    "500":
      description: Internal server error
