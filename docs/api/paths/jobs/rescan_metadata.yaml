post:
  tags:
    - jobs
    - maintenance
  summary: Rescans metadata for every record
  description: |
    Re-runs the metadata parser for every record that has file info. Returns an
    empty object once the scan finishes.
  responses:
    200:
      description: Rescan completed
      content:
        application/json:
          schema:
            type: object
