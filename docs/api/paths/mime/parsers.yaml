get:
  tags:
    - mime
  summary: Lists loaded MIME parsers
  description: |
    Dumps the current MIME database: the loaded parser modules and the types
    each one handles.
  responses:
    200:
      description: MIME database dump
      content:
        application/json:
          schema:
            type: object
