post:
  tags:
    - mime
    - maintenance
  summary: Reloads MIME parsers from disk
  description: |
    Returns the same body as `GET /mime/parsers`.
  responses:
    200:
      description: The parser list after the reload
      content:
        application/json:
          schema:
            type: object
