description: The two halves of a tag, each given as an ID or as text.
type: object
properties:
  namespace:
    $ref: 'TagComponent.yaml'
    example: 1
  subtag:
    $ref: 'TagComponent.yaml'
    example: "toujou koneko"
