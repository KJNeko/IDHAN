# esbuild's postinstall places its native binary, which Vite needs to bundle. pnpm blocks
# dependency build scripts by default and fails the install outright, so it has to be allowed
# explicitly here — otherwise the build depends on someone having run `pnpm approve-builds`
# interactively, which CI and Docker cannot do.
allowBuilds:
  esbuild: true
