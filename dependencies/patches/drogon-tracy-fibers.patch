diff --git a/lib/inc/drogon/utils/coroutine.h b/lib/inc/drogon/utils/coroutine.h
index 8b1dda55..7c054161 100644
--- a/lib/inc/drogon/utils/coroutine.h
+++ b/lib/inc/drogon/utils/coroutine.h
@@ -27,6 +27,10 @@
 #include <type_traits>
 #include <optional>

+#ifdef TRACY_ENABLE
+#include <idhan_tracy/CoroFiber.hpp>
+#endif
+
 namespace drogon
 {
 namespace internal
@@ -201,10 +205,17 @@ struct [[nodiscard]] Task
             return Task<T>{handle_type::from_promise(*this)};
         }

+#ifdef TRACY_ENABLE
+        auto initial_suspend() noexcept
+        {
+            return idhan::tracy_coro::FiberInitialAwaiter{tracyFiberName_.c_str()};
+        }
+#else
         std::suspend_always initial_suspend()
         {
             return {};
         }
+#endif

         void return_value(const T &v)
         {
@@ -216,10 +227,17 @@ struct [[nodiscard]] Task
             value = std::move(v);
         }

+#ifdef TRACY_ENABLE
+        auto final_suspend() noexcept
+        {
+            return idhan::tracy_coro::FiberFinalAwaiter<final_awaiter>{final_awaiter{}};
+        }
+#else
         auto final_suspend() noexcept
         {
             return final_awaiter{};
         }
+#endif

         void unhandled_exception()
         {
@@ -247,9 +265,21 @@ struct [[nodiscard]] Task
             continuation_ = handle;
         }

+#ifdef TRACY_ENABLE
+        template <typename Awaitable>
+        auto await_transform(Awaitable &&awaitable)
+        {
+            return idhan::tracy_coro::FiberAwaiter<Awaitable>{
+                std::forward<Awaitable>(awaitable), tracyFiberName_.c_str()};
+        }
+#endif
+
         std::optional<T> value;
         std::exception_ptr exception_;
         std::coroutine_handle<> continuation_{std::noop_coroutine()};
+#ifdef TRACY_ENABLE
+        std::string tracyFiberName_{idhan::tracy_coro::makeFiberName("drg")};
+#endif
     };

     auto operator co_await() const noexcept
@@ -305,19 +335,33 @@ struct [[nodiscard]] Task<void>
             return Task<>{handle_type::from_promise(*this)};
         }

+#ifdef TRACY_ENABLE
+        auto initial_suspend() noexcept
+        {
+            return idhan::tracy_coro::FiberInitialAwaiter{tracyFiberName_.c_str()};
+        }
+#else
         std::suspend_always initial_suspend()
         {
             return {};
         }
+#endif

         void return_void()
         {
         }

+#ifdef TRACY_ENABLE
+        auto final_suspend() noexcept
+        {
+            return idhan::tracy_coro::FiberFinalAwaiter<final_awaiter>{final_awaiter{}};
+        }
+#else
         auto final_suspend() noexcept
         {
             return final_awaiter{};
         }
+#endif

         void unhandled_exception()
         {
@@ -335,8 +379,20 @@ struct [[nodiscard]] Task<void>
             continuation_ = handle;
         }

+#ifdef TRACY_ENABLE
+        template <typename Awaitable>
+        auto await_transform(Awaitable &&awaitable)
+        {
+            return idhan::tracy_coro::FiberAwaiter<Awaitable>{
+                std::forward<Awaitable>(awaitable), tracyFiberName_.c_str()};
+        }
+#endif
+
         std::exception_ptr exception_;
         std::coroutine_handle<> continuation_{std::noop_coroutine()};
+#ifdef TRACY_ENABLE
+        std::string tracyFiberName_{idhan::tracy_coro::makeFiberName("drg")};
+#endif
     };

     auto operator co_await() const noexcept
